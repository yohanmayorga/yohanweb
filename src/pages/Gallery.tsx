import styles from "@/styles/Gallery.module.css";
import React from "react";

const Gallery = () => {
  return (
    <div id="gallery" className={styles.body}>
      <div className={styles.title}>Gallery</div>
    </div>
  );
};

export default Gallery;
